import { CloseButton } from '@mantine/core';
import * as Styles from './styles';

const Header = () => {
  return (
    <Styles.Wrapper>
      hello
      <CloseButton size="xl" />
    </Styles.Wrapper>
  );
};

export default Header;
