import styled from 'styled-components';

export const Wrapper = styled.div`
  position: absolute;
  left: 0;
  bottom: 0;
  right: 0;
  padding: 20px 24px;
  display: flex;
  justify-content: flex-end;
  gap: 24px;
`;
